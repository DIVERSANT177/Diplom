<% content_for :title, t('dashboards.new.title') %>

<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="page-header">
        <h1><%= t('dashboards.new.title') %></h1>
      </div>

      <div class="card">
        <div class="card-body">
          <%= render "form", dashboard: @dashboard %>
        </div>
      </div>
    </div>
  </div>
</div>
